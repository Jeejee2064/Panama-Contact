import { LAST_VERIFIED } from './config';

// Verbatim disclaimer — must appear byte-identical on every result screen
// (calculator, quiz) and in the generated PDF report.
export const TAX_DISCLAIMER =
  `Estimate only, not legal/tax/accounting advice, consult a licensed CPA/attorney. ` +
  `Panama uses a territorial tax system — foreign-source income is generally exempt. ` +
  `This estimate excludes social security (CSS) and ITBMS. Figures verified ${LAST_VERIFIED}.`;
